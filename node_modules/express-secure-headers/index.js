const compose = require('compose-middleware').compose;
const cors = require('./src/cors');
const crossDomain = require('./src/crossDomain');
const csp = require('./src/csp');
const dns = require('./src/dns');
const frameGuard = require('./src/frameGuard');
const hsts = require('./src/hsts');
const nocahce = require('./src/nocache');
const noSniff = require('./src/noSniff');
const server = require('./src/server');
const XpoweredBy = require('./src/xpoweredby');
const xss = require('./src/xss');

module.exports = compose([
    cors,
    XpoweredBy,
    crossDomain,
    csp,
    dns,
    frameGuard,
    hsts,
    noSniff,
    xss,
    nocahce,
    server,
]);